<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.SRMS" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/onBackground</item>
        <item name="android:textSize">@dimen/text_18</item>
        <item name="android:fontFamily">@font/nunito</item>

        <!-- Splash screen styles -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/background</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@drawable/srms_logo_mini</item>
    </style>

    <style name="Theme.SRMS" parent="Base.Theme.SRMS" />

    <!-- Common Custom Styles -->
    <style name="CustomStyle.TextBox.Red" parent="Widget.AppCompat.TextView">
        <item name="android:padding">15dp</item>
        <item name="android:background">@drawable/rounded_20</item>
        <item name="android:backgroundTint">@color/errorContainer</item>
        <item name="android:textColor">@color/onErrorContainer</item>
    </style>

    <style name="CircleImageShape" parent="Widget.MaterialComponents.ShapeableImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">15%</item>
    </style>
</resources>